name: Health check
on:
  schedule: [{ cron: "*/10 * * * *" }]  # every 10 minutes
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Check health
        run: |
          curl -fsS "https://<your-site>/.netlify/functions/health" | jq .
